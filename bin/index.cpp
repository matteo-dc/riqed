#include "aliases.hpp"

// decompose from linearized index:
//    loop_size must be filled from the inner to the outer index
coords_t decompose(coords_t loop_size)
{
    int size=(int)sizeof(loop_size);
    
    coords_t out
    
}